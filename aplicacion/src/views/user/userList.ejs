<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Capibeers -  Listado de usuarios</title>
    <link rel="stylesheet" href="/css/user.css">
    <link rel="stylesheet" href="css/normalize.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" rel="stylesheet" />
</head>
<body>
    <!--Header/-->
    <%- include('../partials/header')%>
    <main>
        <h2 class="title">LISTADO DE USUARIOS</h2>
        <ul style="text-align: left; margin-left: 50px; margin-bottom: 20px;">
            <% for (let i = 0; i < usersList.length; i++) { %>
            <li>
                <a href="/user/edit/<%= usersList[i].id %>"><%=usersList[i].name  %></a>
                <form method="POST" action="/user/delete/<%=usersList[i].id %>?_method=DELETE">
                    <button type="submit">Eliminar</button>
                </form>
                
            </li>
            <% } %> 
        </ul>

    </main>

    <!--Footer/-->
    <%- include('../partials/footer')%>
</body>
</html>

