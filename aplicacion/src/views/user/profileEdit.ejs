<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Capibeers - Edición de perfil</title>
    <link rel="stylesheet" href="/css/user.css">
    <link rel="stylesheet" href="css/normalize.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" rel="stylesheet" />
</head>
<body>
    <!--Header/-->
        <%- include('../partials/header')%>
    <main>
        <h2 class="title">EDITAR PERFIL</h2>  <!--Editar un perfil in-->
        <div>
            <% for (let i = 0; i < usersList[i].length; i++) { %>
                <p><%=usersList.email %></p>
                <p><%=usersList.name +" "+ usersList.lastName  %></p>
                <p><%=usersList.category %></p>
            <%}%>
        </div>
        <form method="POST" action="/user/edit?_method=PUT" enctype="multipart/form-data">
            <div class="controls"> <!--??? nose por que hay un input. Cambiar a una vista perfil-->
                <input type="text" name="nombre" value="<%=usersList.email %>" id="name" placeholder="Nombre" required>
            </div>
            <div class="buttons">
                <button type="submit" value="Registrar">
                    <p>Continuar</p>
                </button>
            </div>
        </form>
    </main>
    <!--FOOTER/-->
        <%- include('../partials/footer')%>
</body>
</html>